# Core
torch>=2.2
numpy>=1.26
matplotlib>=3.8

# Notebooks
jupyterlab>=4.0
nbformat>=5.9

# The Arcade (interactive app)
streamlit>=1.32

# Optional: RL lesson uses gymnasium when present (built-in fallback otherwise)
gymnasium>=0.29

# Epilogue quest 15 + ship/glyph_oracle.py (auto-installed on Colab if missing)
onnx>=1.15
onnxruntime>=1.17
